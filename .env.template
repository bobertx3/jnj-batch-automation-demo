# Databricks CLI profile used for local OAuth token generation
DATABRICKS_PROFILE=DEFAULT

# Lakebase PostgreSQL connection (auto-populated when deployed to Databricks Apps)
PGHOST=<your-lakebase-instance>.database.cloud.databricks.com
PGPORT=5432
PGDATABASE=batch_release_db
PG_ENDPOINT=projects/<project-id>/branches/production/endpoints/primary
# Optional local override; if unset, app derives user from credential token subject.
# PGUSER=<your-email>@databricks.com
# Optional native Postgres password auth (simplest for long-running apps).
# If PGPASSWORD is set, PGUSER is required and OAuth token flow is bypassed.
# PGUSER=app_batch_release
# PGPASSWORD=<strong-secret>
